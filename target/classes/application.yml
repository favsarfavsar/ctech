server:
  port: 8080
  error:
    include-binding-errors: always
    include-message: always
    include-stacktrace: never

spring:
  datasource:
    url: jdbc:postgresql://snuffleupagus.db.elephantsql.com:5432/qlmucpfw
    username: qlmucpfw
    password: 3hGyBW4lXWY13D5pIbaAk6x82xLgwrFj
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      '[hibernate.format_sql]': true
  mail:
    host: smtp.gmail.com
    port: 587
    username: coderunnersmate@gmail.com
    password: ivnuilnnhujbfzec
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


#image
servlet:
  multipart:
    max-file-size: 2MB
    max-request-size: 2MB


logging:
  file:
    name: log/cookerytech.log
  pattern:
    file: "%d %-5level [%thread] %logger:%msg%n"
  level:
    '[org.springframework.web]' : WARN
    '[org.hibernate]' : INFO
    '[com.cookerytech]' : DEBUG
management: #actuator
  endpoints:
    web:
      exposure:
        include: '*'

cookerytech:
  app:
    jwtSecret: cookerytech@!10
    jwtExpirationMs: 86400000

info:
  application:
    name: 'CookeryTech Backend App'
    description: 'This is CookeryTech Backend Project'
    version: '1.0.0'

